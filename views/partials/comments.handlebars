<div class="card text-white bg-dark mb-3" style="max-width: 18rem;">
    {{#each this}}
    <div>
        {{user.name}} on {{format_date created_at}}
    </div>
    <div>
        {{comment_body}}
    </div>
    {{/each}}
</div>